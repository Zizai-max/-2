# 作品集图片修正规格

## 目标

在不改变页面结构、文案、路由、卡片尺寸和 GitHub Pages 配置的前提下，修正三个图片展示问题：

1. 将原有“AI探索UI视觉设计”章节恢复到“粉丝成长与签到激励体系升级”项目的最底部，排在 19 张新版项目图之后。
2. 用用户提供的蓝紫色 AI 图片替换首页 “AI Workflow Designer” 项目卡片封面。
3. 用用户提供的会员勋章图片替换首页“焕新会员设计语言”项目卡片封面。

## 实现方案

### 粉丝权益项目尾部

- 保留当前 19 张新版项目图及其顺序不变。
- 在当前新版图片章节之后追加一个独立章节：
  - 标题：`AI探索UI视觉设计`
  - 原有说明文案保持不变。
  - 使用现有的 13 张 `public/portfolio/fan-growth/ai-ui-exploration/visual-01.png` 至 `visual-13.png`。
  - 延续原有 `three-up` 三列画廊样式；响应式规则仍由现有项目详情页组件处理。
- 不复制、不删除现有 13 张图片。

### AI 项目卡片封面

- 将用户提供的蓝紫色 AI JPG 原图复制为 `public/portfolio/pages/ai-workflow-cover-2026.jpg`。
- 将 `ai-application` 的 `coverImage` 更新为新路径。
- 保留原 `p18.jpg`，避免删除旧素材，也避免复用旧 URL 带来的缓存问题。

### 会员项目卡片封面

- 将用户提供的会员勋章 PNG 原图复制为 `public/portfolio/pages/member-badge-cover-2026.png`。
- 将 `member-redesign` 的 `coverImage` 更新为新路径。
- 保留原 `p32.jpg`，避免删除旧素材，也避免复用旧 URL 带来的缓存问题。

## 视觉与响应式约束

- 不修改卡片结构、圆角、边距、文字层级、图片容器比例或裁切规则。
- 新封面继续使用当前卡片图片区域的布局行为。
- 粉丝权益新增尾部章节使用项目详情页既有三列画廊，在窄屏下沿用既有响应式降列规则。
- 图片按用户要求保留原文件，不做压缩或重新编码。

## 验证

- 测试首页静态输出引用两个新封面路径。
- 测试粉丝权益详情页中 19 张新版图片在前，随后出现“AI探索UI视觉设计”章节及 13 张旧探索图。
- 验证图片路径顺序、文件存在性和原文件哈希一致性。
- 运行 lint、TypeScript 检查、test、build 与 `git diff --check`。
- 核对 Git diff 不包含路由、部署配置和无关图片修改。

## 非目标

- 不重新设计卡片或项目详情页。
- 不修改项目文案、顺序、路由或导航。
- 不删除旧封面和旧项目资源。
- 不修改 GitHub Pages 工作流或部署方式。
